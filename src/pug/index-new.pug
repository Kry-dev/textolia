<!DOCTYPE html>
html(lang="en")
    include global/head
    +head(
    'TEXtolia'
    )
    include mixin/all-mixin
    body
        .wrapper
            // Sidebar
            nav#sidebar.sidebar
                .sidebar-header
                    a.sidebar-logo(href="index.html")
                        img.logo(src="assets/img/global/logo.png")
                        img.logo-sm(src="assets/img/global/logo-sm.png")
                ul.sidebar-menu.mb-5
                    li.active
                        a(href="#")
                            i.fas.fa-copy
                            span My Texts
                    li
                        a(href="#")
                            i.fas.fa-glasses
                            span My Profile
                    li
                        a(href="#")
                            i.fas.fa-sliders-h
                            span My Statistics
                    li
                        a(href="#")
                            i.fas.fa-keyboard
                            span My Account
                    li
                        a(href="#")
                            i.fas.fa-lightbulb
                            span Log Out
                .sidebar-bottom
                    .view-switch
                        p.mb-0 view-mode:
                        .check-switch
                            input#check1.switch(type='checkbox')
                            label.label-switch(for='check1')
                        .check-switch-text
                            p day
                            p night


            // Page Content
            #content.content
                .content-header
                    h1.content-title
                        span.text-logo Textolia
                        span Your indispensable tool for optimized content
                    button#sidebarCollapse.btn.btn-default(type="button")
                        i.fas.fa-bars
                .content-body
                    // Multi step form
                    section.multi_step_form
                        form#msform
                            // Tittle
                            .tittle
                                h2 Verification Process
                                p In order to use this service, you have to complete this verification process
                            // progressbar
                            ul#progressbar
                                li.active Verify Phone
                                li Upload Documents
                                li Security Questions
                            // fieldsets
                            fieldset
                                h3 Setup your phone
                                h6 We will send you a SMS. Input the code to verify.
                                .form-row
                                    .form-group.col-md-6
                                        input#phone.form-control(type='tel', placeholder='+880')
                                    .form-group.col-md-6
                                        input.form-control(type='text', placeholder='+8801123456789')
                                .done_text
                                    a.don_icon(href='#')
                                        i.ion-android-done
                                    h6
                                        | A secret code is sent to your phone.
                                        br
                                        | Please enter it here.
                                .code_group
                                    input.form-control(type='text', placeholder='0')
                                    input.form-control(type='text', placeholder='0')
                                    input.form-control(type='text', placeholder='0')
                                    input.form-control(type='text', placeholder='0')
                                button.action-button.previous_button(type='button') Back
                                button.next.action-button(type='button') Continue
                            fieldset
                                h3 Verify Your Identity
                                h6 Please upload any of these documents to verify your Identity.
                                .passport
                                    h4
                                        | Govt. ID card
                                        br
                                        | PassPort
                                        br
                                        | Driving License.
                                    a.don_icon(href='#')
                                        i.ion-android-done
                                .input-group
                                    .custom-file
                                        input#upload.custom-file-input(type='file')
                                        label.custom-file-label(for='upload')
                                            i.ion-android-cloud-outline
                                            | Choose file
                                ul.file_added
                                    li File Added:
                                    li
                                        a(href='#')
                                            i.ion-paperclip
                                            | national_id_card.png
                                    li
                                        a(href='#')
                                            i.ion-paperclip
                                            | national_id_card_back.png
                                button.action-button.previous.previous_button(type='button') Back
                                button.next.action-button(type='button') Continue
                            fieldset
                                h3 Create Security Questions
                                h6 Please update your account with security questions
                                .form-group
                                    select.product_select
                                        option(data-display='1. Choose A Question') 1. Choose A Question
                                        option 2. Choose A Question
                                        option 3. Choose A Question
                                .form-group.fg_2
                                    input.form-control(type='text', placeholder='Anwser here:')
                                .form-group
                                    select.product_select
                                        option(data-display='1. Choose A Question') 1. Choose A Question
                                        option 2. Choose A Question
                                        option 3. Choose A Question
                                .form-group.fg_3
                                    input.form-control(type='text', placeholder='Anwser here:')
                                button.action-button.previous.previous_button(type='button') Back
                                a.action-button(href='#') Finish
                    // End Multi step form




    // jQuery CDN - Slim version (=without AJAX)
    script(src='https://code.jquery.com/jquery-3.2.1.min.js')
    // Popper.JS
    script(src='https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js', integrity='sha384-cs/chFZiN24E4KMATLdqdvsezGxaGsi4hLGOzlXwp5UZB1LY//20VyM2taTB4QvJ', crossorigin='anonymous')
    // Bootstrap JS
    script(src='https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js', integrity='sha384-uefMccjFJAIv6A+rW+L4AHf99KvxDjWSu1z9VI8SKNVmz4sk7buKt/6v9KI65qnm', crossorigin='anonymous')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.2/js/bootstrap-select.min.js', crossorigin='anonymous')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-nice-select/1.1.0/js/jquery.nice-select.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js')
    //script(src="assets/js/bundle.js")
    script.
        $(document).ready(function () {
            ;
            (function ($) {
                "use strict";

                //* Form js
                function verificationForm() {
                    //jQuery time
                    var current_fs, next_fs, previous_fs; //fieldsets
                    var left, opacity, scale; //fieldset properties which we will animate
                    var animating; //flag to prevent quick multi-click glitches

                    $(".next").click(function () {
                        if (animating) return false;
                        animating = true;

                        current_fs = $(this).parent();
                        next_fs = $(this).parent().next();

                        //activate next step on progressbar using the index of next_fs
                        $("#progressbar li").eq($("fieldset").index(next_fs)).addClass("active");

                        //show the next fieldset
                        next_fs.show();
                        //hide the current fieldset with style
                        current_fs.animate({
                            opacity: 0
                        }, {
                            step: function (now, mx) {
                                //as the opacity of current_fs reduces to 0 - stored in "now"
                                //1. scale current_fs down to 80%
                                scale = 1 - (1 - now) * 0.2;
                                //2. bring next_fs from the right(50%)
                                left = (now * 50) + "%";
                                //3. increase opacity of next_fs to 1 as it moves in
                                opacity = 1 - now;
                                current_fs.css({
                                    'transform': 'scale(' + scale + ')',
                                    'position': 'absolute'
                                });
                                next_fs.css({
                                    'left': left,
                                    'opacity': opacity
                                });
                            },
                            duration: 800,
                            complete: function () {
                                current_fs.hide();
                                animating = false;
                            },
                            //this comes from the custom easing plugin
                            easing: 'easeInOutBack'
                        });
                    });

                    $(".previous").click(function () {
                        if (animating) return false;
                        animating = true;

                        current_fs = $(this).parent();
                        previous_fs = $(this).parent().prev();

                        //de-activate current step on progressbar
                        $("#progressbar li").eq($("fieldset").index(current_fs)).removeClass("active");

                        //show the previous fieldset
                        previous_fs.show();
                        //hide the current fieldset with style
                        current_fs.animate({
                            opacity: 0
                        }, {
                            step: function (now, mx) {
                                //as the opacity of current_fs reduces to 0 - stored in "now"
                                //1. scale previous_fs from 80% to 100%
                                scale = 0.8 + (1 - now) * 0.2;
                                //2. take current_fs to the right(50%) - from 0%
                                left = ((1 - now) * 50) + "%";
                                //3. increase opacity of previous_fs to 1 as it moves in
                                opacity = 1 - now;
                                current_fs.css({
                                    'left': left
                                });
                                previous_fs.css({
                                    'transform': 'scale(' + scale + ')',
                                    'opacity': opacity
                                });
                            },
                            duration: 800,
                            complete: function () {
                                current_fs.hide();
                                animating = false;
                            },
                            //this comes from the custom easing plugin
                            easing: 'easeInOutBack'
                        });
                    });

                    $(".submit").click(function () {
                        return false;
                    })
                };

                //* Add Phone no select
                function phoneNoselect() {
                    if ($('#msform').length) {
                        $("#phone").intlTelInput();
                        $("#phone").intlTelInput("setNumber", "+880");
                    }
                    ;
                };
                //* Select js
                function nice_Select() {
                    if ($('.product_select').length) {
                        $('select').niceSelect();
                    }
                    ;
                };
                /*Function Calls*/
                verificationForm();
                phoneNoselect();
                nice_Select();
            })(jQuery);
        });
